---
import { getPostUrl } from '../../lib/routes';

const { post } = Astro.props;

const lang = post?.language || 'it';

const siteUrl = 'https://www.retro-gamers.it';

const path = getPostUrl(post);
const url = `${siteUrl}${path}`;

const title = post.seoTitle || post.title || 'Retro-Gamers.it';

const shareLabel = lang === 'en' ? 'Share' : 'Condividi';
const shareOnLabel = lang === 'en' ? 'Share on' : 'Condividi su';

const encodedUrl = encodeURIComponent(url);
const encodedTitle = encodeURIComponent(title);

const shareLinks = [
  {
    label: 'Facebook',
    icon: '/icons/social/facebook.svg',
    href: `https://www.facebook.com/sharer/sharer.php?u=${encodedUrl}`
  },
  {
    label: 'X',
    icon: '/icons/social/x.svg',
    href: `https://twitter.com/intent/tweet?url=${encodedUrl}&text=${encodedTitle}`
  },
  {
    label: 'WhatsApp',
    icon: '/icons/social/whatsapp.svg',
    href: `https://wa.me/?text=${encodedTitle}%20${encodedUrl}`
  },
  {
    label: 'Telegram',
    icon: '/icons/social/telegram.svg',
    href: `https://t.me/share/url?url=${encodedUrl}&text=${encodedTitle}`
  }
];
---

<section class="article-share">
  <span>{shareLabel}</span>

  <div class="article-share__actions">
    {shareLinks.map((item) => (
      <a
        href={item.href}
        target="_blank"
        rel="noopener noreferrer"
        aria-label={`${shareOnLabel} ${item.label}`}
        title={`${shareOnLabel} ${item.label}`}
      >
        <span
          class="article-share__icon"
          style={`--icon: url('${item.icon}')`}
          aria-hidden="true"
        ></span>

        <span class="sr-only">{item.label}</span>
      </a>
    ))}
  </div>
</section>