---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getArticles } from '../lib/sanity';

const posts = await getArticles();
---

<BaseLayout title="Retro Gamers">
  <h1>Retro Gamers</h1>

  {posts.map(post => (
    <div>
      <h2>{post.title}</h2>
    </div>
  ))}
</BaseLayout>