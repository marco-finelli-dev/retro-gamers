---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import PlatformsLanding from '../../../components/platforms/PlatformsLanding.astro';

import { getAllPosts, groupPosts } from '../../../lib/posts';
import { getAllPlatforms } from '../../../lib/platforms';
import { getPlatformIndexUrl, getPlatformTypeUrl } from '../../../lib/routes.js';
import { createCollectionPageStructuredData, createListItems } from '../../../lib/structured-data.js';

const allPosts = await getAllPosts();
const grouped = groupPosts(allPosts);

const englishReviews = allPosts.filter(
  (post) => post.type === 'review' && post.language === 'en'
);

const englishNews = allPosts.filter(
  (post) => post.type === 'news' && post.language === 'en'
);

const platforms = await getAllPlatforms();

const alternateLanguages = [
  { lang: 'it', url: 'https://www.retro-gamers.it/piattaforme/' },
  { lang: 'en', url: 'https://www.retro-gamers.it/en/platforms/' },
  { lang: 'x-default', url: 'https://www.retro-gamers.it/piattaforme/' }
];

const platformTypeItems = [
  { name: 'Arcade', url: getPlatformTypeUrl('arcade', 'en') },
  { name: 'Computers', url: getPlatformTypeUrl('computer', 'en') },
  { name: 'Consoles', url: getPlatformTypeUrl('console', 'en') }
];

const structuredData = createCollectionPageStructuredData({
  url: 'https://www.retro-gamers.it/en/platforms/',
  name: 'Platforms',
  description: 'Consoles, home computers and arcade systems from the history of retro gaming.',
  lang: 'en',
  items: createListItems(platformTypeItems, (item) => item)
});
---

<BaseLayout
  reviews={englishReviews}
  news={englishNews}
  lang="en"
  title="Platforms"
  description="Consoles, home computers and arcade systems from the history of retro gaming."
  canonical={`https://www.retro-gamers.it${getPlatformIndexUrl('en')}`}
  alternateLanguages={alternateLanguages}
  structuredData={structuredData}
>
  <PlatformsLanding platforms={platforms} lang="en" />
</BaseLayout>
