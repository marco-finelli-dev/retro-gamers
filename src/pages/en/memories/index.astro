---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ArchivePage from '../../../components/archive/ArchivePage.astro';

import { getAllPosts, groupPosts } from '../../../lib/posts';
import { PAGE_SIZE } from '../../../lib/pagination';
import { getPostUrl } from '../../../lib/routes.js';
import { createCollectionPageStructuredData, createListItems } from '../../../lib/structured-data.js';

const allPosts = await getAllPosts();
const grouped = groupPosts(allPosts);

const englishReviews = allPosts.filter(
  (post) => post.type === 'review' && post.language === 'en'
);

const englishNews = allPosts.filter(
  (post) => post.type === 'news' && post.language === 'en'
);

const posts = grouped.english.filter((post) => post.type === 'memories');

const alternateLanguages = [
  { lang: 'it', url: 'https://www.retro-gamers.it/memories/' },
  { lang: 'en', url: 'https://www.retro-gamers.it/en/memories/' },
  { lang: 'x-default', url: 'https://www.retro-gamers.it/memories/' }
];

const structuredData = createCollectionPageStructuredData({
  url: 'https://www.retro-gamers.it/en/memories/',
  name: 'Video game memories',
  description: 'Personal stories and memories connected to video games, arcades, consoles, home computers, magazines and digital culture.',
  lang: 'en',
  items: createListItems(posts.slice(0, PAGE_SIZE), (post) => ({
    name: post.title,
    url: getPostUrl(post)
  }))
});
---

<BaseLayout
  reviews={englishReviews}
  news={englishNews}
  title="Video game memories"
  description="Personal stories and memories connected to video games, arcades, consoles, home computers, magazines and digital culture."
  image="/og-image.jpg"
  canonical="https://www.retro-gamers.it/en/memories/"
  lang="en"
  alternateLanguages={alternateLanguages}
  structuredData={structuredData}
>
  <ArchivePage
    title="Video game memories"
    eyebrow="English edition"
    description="Memories collects personal stories connected to video games and the digital culture that surrounded them. Arcades, shops, magazines, home computers, consoles received as gifts and games discovered almost by accident become starting points for experiences shared across generations. This is not simply a celebration of nostalgia, but a space where individual memory meets the history of the places, habits and people that shaped our relationship with video games."
    posts={posts}
    showFilters={false}
    lang="en"
  />
</BaseLayout>
